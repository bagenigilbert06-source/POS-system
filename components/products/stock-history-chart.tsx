'use client'

import { Area, AreaChart, CartesianGrid, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'

export function StockHistoryChart({ data, unit, alertLevel }: { data: Array<{ date: string; stock: number }>; unit: string; alertLevel?: number }) {
  if (data.length === 0) return <p className="py-8 text-sm text-muted-foreground">Stock history will appear after the first movement.</p>
  return <div className="space-y-3 font-sans" role="img" aria-label={`Stock level trend in ${unit}`}><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-semibold text-[#101828] dark:text-slate-100">Inventory trend</p><p className="text-xs text-muted-foreground">Latest stock movement history</p></div>{alertLevel !== undefined && <span className="rounded-full border border-amber-200 bg-amber-50 px-2.5 py-1 text-xs font-medium text-amber-800 dark:border-amber-900/70 dark:bg-amber-950/35 dark:text-amber-300">Alert at {alertLevel} {unit}</span>}</div><div className="h-64 w-full"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{ top: 14, right: 12, left: -20, bottom: 4 }}><defs><linearGradient id="stock-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#e0ad00" stopOpacity={0.28} /><stop offset="100%" stopColor="#e0ad00" stopOpacity={0.03} /></linearGradient></defs><CartesianGrid vertical={false} stroke="hsl(var(--border))" strokeDasharray="3 3" /><XAxis dataKey="date" tick={{ fontSize: 11, fill: 'hsl(var(--muted-foreground))' }} tickLine={false} axisLine={false} /><YAxis allowDecimals={false} tick={{ fontSize: 11, fill: 'hsl(var(--muted-foreground))' }} tickLine={false} axisLine={false} /><Tooltip cursor={{ stroke: 'hsl(var(--muted-foreground))', strokeDasharray: '4 4' }} formatter={(value) => [`${value} ${unit}`, 'Available']} contentStyle={{ backgroundColor: 'hsl(var(--card))', border: '1px solid hsl(var(--border))', borderRadius: 8, boxShadow: '0 10px 24px rgba(0,0,0,.24)', color: 'hsl(var(--foreground))', fontSize: 12 }} labelStyle={{ color: 'hsl(var(--muted-foreground))' }} itemStyle={{ color: '#e0ad00', fontWeight: 600 }} />{alertLevel !== undefined && <ReferenceLine y={alertLevel} stroke="#d97706" strokeDasharray="5 5" label={{ value: 'Alert level', position: 'insideTopRight', fill: '#d97706', fontSize: 11 }} />}<Area type="monotone" dataKey="stock" stroke="#c99500" strokeWidth={2.5} fill="url(#stock-fill)" dot={{ r: 3, fill: '#c99500', strokeWidth: 2, stroke: 'hsl(var(--card))' }} activeDot={{ r: 5, fill: '#e0ad00', stroke: 'hsl(var(--card))', strokeWidth: 2 }} /></AreaChart></ResponsiveContainer></div></div>
}
