'use client'
import { useMemo, useState, useTransition } from 'react'
import { importCategoriesFromCsv, previewCategoryImport } from '@/app/actions/category-import'
import { notify } from '@/lib/notify'
const headers = ['name', 'ageRestricted', 'isActive']; const example = ['Whisky', 'true', 'true']
function parseCsv(text: string) { const rows: string[][] = []; let row: string[] = [], cell = '', quoted = false; for (let i = 0; i < text.length; i++) { const char = text[i]; if (char === '"' && quoted && text[i + 1] === '"') { cell += '"'; i++ } else if (char === '"') quoted = !quoted; else if (char === ',' && !quoted) { row.push(cell.trim()); cell = '' } else if ((char === '\n' || char === '\r') && !quoted) { if (char === '\r' && text[i + 1] === '\n') i++; row.push(cell.trim()); if (row.some(Boolean)) rows.push(row); row = []; cell = '' } else cell += char }; row.push(cell.trim()); if (row.some(Boolean)) rows.push(row); return rows }
export function CategoryCsvImporter() { const [text, setText] = useState(`${headers.join(',')}\n${example.join(',')}`), [preview, setPreview] = useState<Awaited<ReturnType<typeof previewCategoryImport>> | null>(null), [pending, startTransition] = useTransition(); const parsed = useMemo(() => { const data = parseCsv(text); if (!data.length) return { rows: [] as Array<{ rowNumber: number; name: string; ageRestricted: string; isActive: string }>, error: 'CSV is empty.' }; const index = new Map(data[0].map((name, position) => [name.trim(), position])); const missing = headers.filter((header) => !index.has(header)); if (missing.length) return { rows: [] as Array<{ rowNumber: number; name: string; ageRestricted: string; isActive: string }>, error: `Missing columns: ${missing.join(', ')}` }; return { rows: data.slice(1, 201).map((cells, offset) => ({ rowNumber: offset + 2, name: cells[index.get('name')!] || '', ageRestricted: cells[index.get('ageRestricted')!] || '', isActive: cells[index.get('isActive')!] || '' })), error: data.length > 201 ? 'Maximum import size is 200 rows.' : undefined } }, [text]); const previewRows = () => startTransition(async () => { try { if (parsed.error) throw new Error(parsed.error); setPreview(await previewCategoryImport({ rows: parsed.rows })) } catch (error) { notify.error(error instanceof Error ? error.message : 'Unable to preview CSV') } }); const confirm = () => startTransition(async () => { try { const result = await importCategoriesFromCsv({ rows: parsed.rows }); notify.success(`${result.imported} categories imported`); setPreview(null) } catch (error) { notify.error(error instanceof Error ? error.message : 'Import failed', { duration: 10000 }) } }); return <div className="space-y-4"><section className="rounded-xl border bg-card p-4"><h2 className="text-sm font-semibold">CSV category import</h2><p className="mt-1 text-xs text-muted-foreground">Create-only import, maximum 200 categories.</p><input type="file" accept=".csv,text/csv" className="mt-4 block text-xs" onChange={async (event) => { const file = event.target.files?.[0]; if (file) { setText(await file.text()); setPreview(null) } }} /><textarea value={text} onChange={(event) => { setText(event.target.value); setPreview(null) }} rows={12} spellCheck={false} className="mt-4 w-full rounded-lg border bg-background p-3 font-mono text-[11px]" /></section>{parsed.error && <p className="rounded-lg border border-destructive/30 bg-destructive/5 p-3 text-sm text-destructive">{parsed.error}</p>}<p className="rounded-xl border bg-card p-4 text-sm"><b>{parsed.rows.length}</b> rows ready. Supported columns: {headers.join(', ')}.</p>{preview && <section className="rounded-xl border bg-card p-4"><div className="flex gap-5 text-sm"><span><b>{preview.validRows}</b> valid</span><span><b>{preview.invalidRows}</b> invalid</span></div>{preview.errors.length > 0 && <div className="mt-3 max-h-64 space-y-1 overflow-auto rounded-lg border border-destructive/30 bg-destructive/5 p-3 text-xs text-destructive">{preview.errors.map((error, index) => <p key={`${error.rowNumber}-${index}`}>Row {error.rowNumber}: {error.message}</p>)}</div>}</section>}<div className="flex justify-end gap-2"><button disabled={pending || Boolean(parsed.error) || !parsed.rows.length} onClick={previewRows} className="rounded-lg border px-4 py-2.5 text-sm font-semibold disabled:opacity-40">{pending ? 'Checking…' : 'Validate & preview'}</button>{preview && <button disabled={pending || preview.invalidRows > 0 || preview.validRows === 0} onClick={confirm} className="rounded-lg bg-foreground px-4 py-2.5 text-sm font-semibold text-background disabled:opacity-40">Import categories</button>}</div></div> }
