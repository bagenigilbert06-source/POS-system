'use client'

import { useMemo, useState } from 'react'
import Image from 'next/image'
import { createCategory, setCategoryActive, updateCategory } from '@/app/actions/categories'
import { Check, Pencil, Plus, Search, RotateCcw } from 'lucide-react'
import { toast } from 'sonner'

type Category = { id: string; name: string; slug: string; description: string | null; imageUrl: string | null; parentCategoryId: string | null; isActive: boolean; createdAt: Date; updatedAt: Date; productCount: number }

export function CategoriesClient({ initialCategories }: { initialCategories: Category[] }) {
  const [categories, setCategories] = useState(initialCategories)
  const [search, setSearch] = useState('')
  const [editing, setEditing] = useState<Category | null>(null)
  const filtered = useMemo(() => categories.filter((item) => item.name.toLowerCase().includes(search.toLowerCase())), [categories, search])
  const parentName = (id: string | null) => categories.find((item) => item.id === id)?.name
  const save = async (form: { name: string; description: string; parentCategoryId: string; imageUrl?: string | null }) => {
    try {
      if (editing?.id) { await updateCategory(editing.id, { ...form, parentCategoryId: form.parentCategoryId || null }); toast.success('Category updated') }
      else { await createCategory({ ...form, parentCategoryId: form.parentCategoryId || null }); toast.success('Category created') }
      window.location.reload()
    } catch (error) { toast.error(error instanceof Error ? error.message : 'Could not save category') }
  }
  return <div className="space-y-4"><div className="grid gap-3 sm:grid-cols-3"><div className="rounded-lg border bg-white px-4 py-3"><p className="text-xs text-muted-foreground">Total categories</p><p className="mt-1 text-xl font-bold">{categories.length}</p></div><div className="rounded-lg border bg-white px-4 py-3"><p className="text-xs text-muted-foreground">Active categories</p><p className="mt-1 text-xl font-bold text-[#28743c]">{categories.filter((item) => item.isActive).length}</p></div><div className="rounded-lg border bg-white px-4 py-3"><p className="text-xs text-muted-foreground">Categories in use</p><p className="mt-1 text-xl font-bold">{categories.filter((item) => item.productCount > 0).length}</p></div></div><div className="flex flex-wrap items-center justify-between gap-3"><div className="relative w-full max-w-sm"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search categories" aria-label="Search categories" className="w-full rounded-md border bg-background py-2 pl-9 pr-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/20" /></div><button onClick={() => setEditing({ id: '', name: '', slug: '', description: '', imageUrl: null, parentCategoryId: null, isActive: true, createdAt: new Date(), updatedAt: new Date(), productCount: 0 })} className="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground"><Plus className="h-4 w-4" />Create category</button></div><div className="overflow-hidden rounded-xl border bg-white"><table className="w-full text-sm"><thead className="border-b bg-muted/40 text-left text-muted-foreground"><tr><th className="px-4 py-3">Category</th><th className="px-4 py-3">Parent</th><th className="px-4 py-3">Products</th><th className="px-4 py-3">Status</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody>{filtered.map((item) => <tr key={item.id} className="border-b last:border-0 hover:bg-muted/20"><td className="px-4 py-3"><div className="flex items-center gap-3"><div className="h-9 w-9 overflow-hidden rounded-md bg-[#fff8e8]">{item.imageUrl ? <Image src={item.imageUrl} alt="" width={36} height={36} unoptimized className="h-full w-full object-cover" /> : <span className="flex h-full items-center justify-center text-xs text-[#9a6900]">#</span>}</div><div><p className="font-semibold">{item.name}</p><p className="text-xs text-muted-foreground">/{item.slug}</p></div></div></td><td className="px-4 py-3 text-muted-foreground">{parentName(item.parentCategoryId) ?? 'Top-level category'}</td><td className="px-4 py-3 font-medium">{item.productCount}</td><td className="px-4 py-3"><span className={`inline-flex rounded-full px-2 py-1 text-xs font-semibold ${item.isActive ? 'bg-[#edf7ef] text-[#28743c]' : 'bg-muted text-muted-foreground'}`}>{item.isActive ? 'Active' : 'Archived'}</span></td><td className="px-4 py-3 text-right"><button onClick={() => setEditing(item)} className="rounded p-2 text-muted-foreground hover:bg-muted" aria-label={`Edit ${item.name}`}><Pencil className="h-4 w-4" /></button>{item.slug !== 'uncategorised' && <button onClick={async () => { try { await setCategoryActive(item.id, !item.isActive); window.location.reload() } catch (error) { toast.error(error instanceof Error ? error.message : 'Could not update category') } }} className="rounded p-2 text-muted-foreground hover:bg-muted" aria-label={item.isActive ? `Archive ${item.name}` : `Restore ${item.name}`}>{item.isActive ? <RotateCcw className="h-4 w-4" /> : <Check className="h-4 w-4" />}</button>}</td></tr>)}</tbody></table>{filtered.length === 0 && <p className="p-8 text-center text-sm text-muted-foreground">No categories found.</p>}</div>{editing && <CategoryModal category={editing.id ? editing : null} categories={categories} onClose={() => setEditing(null)} onSave={save} />}</div>
}

function CategoryModal({ category, categories, onClose, onSave }: { category: Category | null; categories: Category[]; onClose: () => void; onSave: (form: { name: string; description: string; parentCategoryId: string; imageUrl?: string | null }) => Promise<void> }) {
  const [name, setName] = useState(category?.name ?? '')
  const [description, setDescription] = useState(category?.description ?? '')
  const [parentCategoryId, setParentCategoryId] = useState(category?.parentCategoryId ?? '')
  const [step, setStep] = useState(1)
  const [saving, setSaving] = useState(false)
  const [imageUrl, setImageUrl] = useState(category?.imageUrl ?? '')
  const [uploading, setUploading] = useState(false)
  const inputClass = 'mt-1 w-full rounded-md border bg-background px-3 py-2 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/20'
  const uploadImage = async (file?: File) => { if (!file) return; if (file.size > 5 * 1024 * 1024) return toast.error('Image is too large. Choose an image smaller than 5 MB.'); setUploading(true); try { const body = new FormData(); body.append('file', file); const response = await fetch('/api/products/images', { method: 'POST', body }); const result = await response.json(); if (!response.ok) throw new Error(result.error || 'Image upload failed'); setImageUrl(result.url); toast.success('Category image uploaded') } catch (error) { toast.error(error instanceof Error ? error.message : 'Image upload failed') } finally { setUploading(false) } }
  const submit = async () => { setSaving(true); try { await onSave({ name, description, parentCategoryId, imageUrl: imageUrl || null }) } finally { setSaving(false) } }
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><form onSubmit={(event) => { event.preventDefault(); if (step < 3) setStep((current) => current + 1); else void submit() }} className="w-full max-w-md overflow-hidden rounded-xl border bg-card shadow-xl"><div className="border-b px-5 py-4"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-wide text-primary">Step {step} of 3</p><h2 className="mt-1 text-lg font-bold">{category ? 'Edit category' : 'Create category'}</h2><p className="mt-1 text-sm text-muted-foreground">{step === 1 ? 'Name your category.' : step === 2 ? 'Place it in your catalogue.' : 'Review before saving.'}</p></div><button type="button" onClick={onClose} className="text-xl text-muted-foreground" aria-label="Close">×</button></div><div className="mt-4 grid grid-cols-3 gap-1">{[1, 2, 3].map((item) => <span key={item} className={`h-1 rounded-full ${item <= step ? 'bg-primary' : 'bg-muted'}`} />)}</div></div><div className="p-5">{step === 1 && <label className="block text-sm font-medium">Category name<input required autoFocus value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Spirits" className={inputClass} /></label>}{step === 2 && <div className="space-y-4"><label className="block text-sm font-medium">Parent category <span className="font-normal text-muted-foreground">(optional)</span><select value={parentCategoryId} onChange={(event) => setParentCategoryId(event.target.value)} className={inputClass}><option value="">Top-level category</option>{categories.filter((item) => item.isActive && item.id !== category?.id).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="block text-sm font-medium">Description <span className="font-normal text-muted-foreground">(optional)</span><textarea rows={3} value={description} onChange={(event) => setDescription(event.target.value)} placeholder="Help your team understand this group." className={inputClass} /></label><div><label className="block text-sm font-medium">Category image <span className="font-normal text-muted-foreground">(optional)</span></label><div className="mt-2 flex items-center gap-3"><div className="h-16 w-16 overflow-hidden rounded-md bg-[#fff8e8]">{imageUrl ? <Image src={imageUrl} alt="Category preview" width={64} height={64} unoptimized className="h-full w-full object-cover" /> : <span className="flex h-full items-center justify-center text-[#9a6900]">#</span>}</div><div><input id="category-image" type="file" accept="image/jpeg,image/png,image/webp" onChange={(event) => void uploadImage(event.target.files?.[0])} className="sr-only" /><label htmlFor="category-image" className="cursor-pointer rounded-md border bg-background px-3 py-2 text-sm font-medium">{uploading ? 'Uploading…' : 'Upload image'}</label>{imageUrl && <button type="button" onClick={() => setImageUrl('')} className="ml-2 text-xs text-destructive">Remove</button>}</div></div></div></div>}{step === 3 && <div className="rounded-lg border bg-muted/30 p-4 text-sm"><p className="text-xs text-muted-foreground">Category name</p><p className="font-semibold">{name}</p><p className="mt-3 text-xs text-muted-foreground">Parent</p><p>{parentCategoryId ? categories.find((item) => item.id === parentCategoryId)?.name : 'Top-level category'}</p>{imageUrl && <Image src={imageUrl} alt="" width={80} height={80} unoptimized className="mt-3 h-20 w-20 rounded-md object-cover" />}{description && <><p className="mt-3 text-xs text-muted-foreground">Description</p><p>{description}</p></>}</div>}</div><div className="flex justify-between border-t bg-muted/20 px-5 py-4"><button type="button" onClick={step === 1 ? onClose : () => setStep((current) => current - 1)} className="rounded-md border bg-background px-3 py-2 text-sm font-medium">{step === 1 ? 'Cancel' : 'Back'}</button><button disabled={saving || uploading || (step === 1 && !name.trim())} className="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground disabled:opacity-50">{saving ? 'Saving…' : step === 3 ? 'Save category' : 'Continue'}</button></div></form></div>
}
