import 'dotenv/config'; import pg from 'pg'; import crypto from 'node:crypto';
const url=process.env.DIRECT_URL||process.env.DATABASE_URL; const pool=new pg.Pool({connectionString:url,ssl:url?.includes('supabase')?{rejectUnauthorized:false}:undefined}); const c=await pool.connect();
try { const o=await c.query('select id from organization where id=$1 or $1 is null order by "createdAt" asc limit 1',[process.env.ORG_ID||null]); if(!o.rows[0]) throw new Error('No organization found'); const id=crypto.randomUUID(); await c.query(`insert into promotion_rule (id,"organizationId",name,code,description,kind,"valueType",value,"minimumSpend","maximumDiscount","usageLimit","startsAt","endsAt","isActive") values ($1,$2,'Welcome Customer Coupon','WELCOME500','Save KES 500 when you spend KES 5,000.','coupon','fixed',500,5000,500,100,now(),now()+interval '30 days',true)`,[id,o.rows[0].id]); console.log('Seeded coupon WELCOME500'); } finally { c.release(); await pool.end(); }
