'use client'

import { Area, AreaChart, CartesianGrid, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'

export function StockHistoryChart({ data, unit, alertLevel }: { data: Array<{ date: string; stock: number }>; unit: string; alertLevel?: number }) {
  if (data.length === 0) return <p className="py-8 text-sm text-muted-foreground">Stock history will appear after the first movement.</p>
  return <div className="space-y-3" role="img" aria-label={`Stock level trend in ${unit}`}><div className="flex items-center justify-between"><div><p className="text-sm font-semibold text-[#101828]">Inventory trend</p><p className="text-xs text-muted-foreground">Latest stock movement history</p></div>{alertLevel !== undefined && <span className="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-medium text-amber-800">Alert at {alertLevel} {unit}</span>}</div><div className="h-64 w-full"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{ top: 14, right: 12, left: -20, bottom: 4 }}><defs><linearGradient id="stock-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#e0ad00" stopOpacity={0.28} /><stop offset="100%" stopColor="#e0ad00" stopOpacity={0.03} /></linearGradient></defs><CartesianGrid vertical={false} stroke="#edf0f4" /><XAxis dataKey="date" tick={{ fontSize: 11, fill: '#667085' }} tickLine={false} axisLine={false} /><YAxis allowDecimals={false} tick={{ fontSize: 11, fill: '#667085' }} tickLine={false} axisLine={false} /><Tooltip cursor={{ stroke: '#d9a900', strokeDasharray: '4 4' }} formatter={(value) => [`${value} ${unit}`, 'Available']} contentStyle={{ borderRadius: 10, border: '1px solid #dfe3ea', boxShadow: '0 8px 24px rgba(16,24,40,.10)', fontSize: 12 }} />{alertLevel !== undefined && <ReferenceLine y={alertLevel} stroke="#d97706" strokeDasharray="5 5" label={{ value: 'Alert level', position: 'insideTopRight', fill: '#b45309', fontSize: 11 }} />}<Area type="monotone" dataKey="stock" stroke="#c99500" strokeWidth={2.5} fill="url(#stock-fill)" dot={{ r: 3, fill: '#c99500', strokeWidth: 2, stroke: '#fff' }} activeDot={{ r: 5 }} /></AreaChart></ResponsiveContainer></div></div>
}
